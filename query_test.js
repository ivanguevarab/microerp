const { createClient } = require('@supabase/supabase-js');
// Necesito la URL y KEY de Supabase, pero no la tengo en bash fácilmente sin leer config.js.
// Mejor busco la tabla en otros archivos HTML.
